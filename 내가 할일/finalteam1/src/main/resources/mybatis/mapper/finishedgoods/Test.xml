<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finalproject.finalteam1.mapper.finishedgoods.Test">

    <select id="materialDivision" resultType="com.finalproject.finalteam1.domain.test.TestVO">
        SELECT material_division
        FROM MATERIALS
        GROUP BY MATERIAL_DIVISION
    </select>

    <select id="materialName" resultType="com.finalproject.finalteam1.domain.test.TestVO">
        SELECT MATERIAL_NAME
        FROM MATERIALS
        WHERE MATERIAL_DIVISION = #{mt.material_name}
    </select>

</mapper>